# ملخص جلسة العمل الثانية — منصة بكجات (PKGAT)

> مستند تسليم لوكيل آخر. يكمل مستند الجلسة الأولى ويحل محله.

---

## السياق الأساسي

**المشروع:** منصة SaaS سعودية للدعوات الإلكترونية بباركود دخول.

| | |
|---|---|
| المستودع | `iMEiD/Pkgat` |
| فرع النشر (الافتراضي) | `claude/pkgat-invitation-platform-2kb9s1` — **هذا ما ينشره Vercel** |
| فرع العمل | `claude/new-session-uuznpl` |
| آخر كومِت | `ec3f4aa` — **الفرعان متطابقان** |
| Supabase | `vjkhgaogqgiobvhyiswp` |
| النطاق | `pkgat.com` — مربوط بـ Vercel ويعمل |
| البريد | Resend مربوط كـ Custom SMTP في Supabase ✅ |
| المستخدم | `xxm4il@gmail.com` — الأدمن الوحيد، **غير تقني ويعمل من الجوال** |
| اللغة | **كل شيء بالعربية**: الواجهة، التعليقات، رسائل الـ commit، والتواصل |

**⚠️ سير العمل الإلزامي:** بعد كل كومِت على فرع العمل، ادمجه في فرع النشر وادفع الاثنين:

```bash
git push -u origin claude/new-session-uuznpl
git checkout claude/pkgat-invitation-platform-2kb9s1 -q
git merge --ff-only claude/new-session-uuznpl -q
git push origin claude/pkgat-invitation-platform-2kb9s1
git checkout claude/new-session-uuznpl -q
```

بدون هذا **لا يصل شيء للموقع**.

---

## ⚠️ معلّق على المستخدم الآن

| البند | الحالة |
|---|---|
| **ترحيل `0012` (الخطوط)** | ❓ لم يؤكّد تنفيذه — بدونه صفحة `/admin/fonts` لا تحفظ |
| **قوالب البريد** | أُرسلت الأربعة للّصق في Supabase — لم يؤكّد |
| **خطأ إرسال الاقتراح** | ما زال يفشل عنده. الرسالة صارت تعرض **خطأ قاعدة البيانات نصّاً** — اطلب منه النص |

الترحيلات `0006`–`0011` **نُفّذت وأكّدها**.

---

## 🔴 أخطاء حقيقية اكتُشفت — والسبب كان دائماً غير المتوقّع

| ما أبلغ عنه | السبب الحقيقي |
|---|---|
| «العضوية ما تفك حد الدعوات» | `guest_code_state` في SQL **لا تعرف الاشتراكات إطلاقاً** — تعرف `free_quota` فقط. الواجهة تحسب الحد صح فتسمح بالإضافة، وقاعدة البيانات تُبطل الباركودات. **الباب يتبع قاعدة البيانات.** أُصلح في `0009` بدالة `guest_over_limit` |
| «الإعدادات تقول مفعّلة والمدعوون غير مفعّلين» | التفعيل اليدوي يتجاوز **التوقيت وحده** — حدّ الباقة يبقى. السلوك صحيح، **النص المعروض كان يكذب** |
| «الميزات ما ظهرت» رغم الدفع | `vercel.json` بجدولة cron **ساعية**، وباقة Hobby تسمح بيومية واحدة → **Vercel يرفض النشر كاملاً ولا ينشئ سجل نشرة**. ٨ كومِتات بقيت بلا نشر. ⚠️ **زر Redeploy يعيد بناء نفس الكومِت القديم — لا يسحب الجديد** |
| «صفحة الاقتراحات تنهار» | ملف `'use server'` **لا يجوز أن يُصدّر إلا دوالاً غير متزامنة** — صدّرت منه ثابتاً. **البناء ينجح ثم تسقط الصفحة وقت التشغيل** |
| «التسجيل يرفض Gmail ويقبل بريداً مسجّلاً» | SMTP معطّل: البريد الجديد يحتاج إرسالاً فيفشل، والمسجّل لا يُرسل له شيء فينجح ظاهرياً. والسبب كان **`Resend` بحرف كبير** في خانة Username (كيبورد الجوال يرفع أول حرف) |
| «المسح ما يشتغل» | مناسبة بتاريخ ماضٍ → كل الباركودات `expired`. أُضيف منع التواريخ الماضية |
| «الوضع الليلي ألوانه غلط» | ألوان التنبيهات ثابتة لا متغيرات؛ و`bg-ink` + `text-white` يصير **أبيض على أبيض** لأن `ink` ينقلب |
| «النص الإضافي يختفي» | `fillText` يرسم **سطراً واحداً فقط** |

---

## ما أُنجز في هذه الجلسة

**إصلاحات:** حد الباقة يحترم العضوية · التواريخ الماضية · لوحة المسح تملأ الشاشة والنتيجة تختفي تلقائياً · رسائل تسجيل صادقة · كشف البريد المكرر · منع الدخول بلا تفعيل · جدولة cron

**ميزات:** ٦ قوالب جاهزة (مولّدة برمجياً في `scripts/`) · الشروط والخصوصية + موافقة PDPL · حذف الحساب · تصدير Excel · تمييز المتأخرين · تنبيه مسؤولي المسح · مناسبة تجريبية · تذكير ٢٤ ساعة · **الوضع الليلي** · **صندوق اقتراحات مفتوح للزوار** · مشاركة التصاميم + عرض متحرك · **نصوص إضافية حرة متعددة الأسطر** · **رفع خطوط** · منح العضويات والحصص · جوال إلزامي بمفتاح دولة وفريد · قوالب بريد عربية

---

## أسلوب العمل المتوقّع

**١. تحقّق قبل الإصلاح — السبب مختلف دائماً.** كل بق في هذه الجلسة كان سببه غير ما بدا.

**٢. شغّل PostgreSQL حقيقي لاختبار SQL** (`/usr/lib/postgresql/16/bin`, تحتاج `su postgres`). اختبرت به RLS وحدود الباقات وتفرّد الجوال — واكتشفت أخطاء ما كانت لتظهر بالقراءة.

**٣. شغّل Playwright** (`/opt/pw-browsers/chromium`) للتحقق البصري. أنشئ مسار معاينة مؤقت، تحقق، **ثم احذفه**. ⚠️ مجلد يبدأ بـ `_` **لا يُوجَّه إليه في Next.js**.

**٤. لا تخفِ الأخطاء خلف رسائل عامة.** «تعذّر الإرسال. حاول مرة أخرى» أضاعت جلسة كاملة.

**٥. بعد كل تعديل:** `npx tsc --noEmit` ثم `npm run build`. ⚠️ **البناء لا يمسك أخطاء `'use server'`** — تظهر وقت التشغيل فقط.

**٦. التواصل:** خطوات مرقّمة، **كود جاهز للّصق كاملاً في نص الرسالة** (المستودع خاص فروابط GitHub تعطيه 404)، وخطوة واحدة في كل رسالة.

---

## البيئة

- المتغيرات للاختبار المحلي: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SCANNER_SESSION_SECRET`, `ADMIN_SESSION_SECRET` (٣٢ حرفاً+), `NEXT_PUBLIC_SITE_URL`, `RESEND_API_KEY`, `EMAIL_FROM`, `CRON_SECRET`
- Google Fonts و`pkgat.com` **محجوبان في بيئة التطوير** — لا تحاول جلبهما
- الترحيلات `0001`–`0012` في `supabase/migrations/`

---

## المقترح بعد ذلك

**قبل الإطلاق:** تنظيف الأرقام المكررة إن وُجدت · اختبار مسح حقيقي (توليد ٥ دعوات، مسح، تكرار، إنهاء) · ملء المعرض بأعمال حقيقية

**مؤجّل عمداً:** مُيسّر (لا يحتاجها للتجربة المجانية) · الباقات الزائدة (٣ باقات دفع واحد تربك المشتري) · إرسال واتساب

**نُصح بوضوح:** لا يطلق قبل مناسبة حقيقية **يحضرها بنفسه**، و١٠-١٥ مجرّباً يعرفهم لا إعلاناً عاماً.
